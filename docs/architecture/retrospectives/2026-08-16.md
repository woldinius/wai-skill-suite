## Suite retrospective: wai-skill-suite · 2026-08-16

**Period:** all recorded rows → today · what set the anchor: no prior report marker (this is the
maiden retro — the marker this run plants is the first)
**Suite version:** not stamped (`.claude/.wai-suite-version` absent — expected: this repo is the
suite's source, not an install target; the stamp is written by `install.sh`)
**Trigger:** threshold — 26 verdicts since the last report marker (none), threshold 25 (doctor's
advisory: "a report is due at 25")

### The numbers (script-derived, pasted verbatim)

```
## Gate report — 2026-08-16

Ledger: `docs/architecture/gate-ledger.md` · period covered: 2026-08-05 → 2026-08-14

- verdicts: 26 — GO 3 · NO-GO 22 · UNKNOWN 1 · MOOT 0
- outcome coverage: 26 of 26 tagged (100%) · 0 untagged
- 1 unmatched tag(s): need inspection — these rows are in NO rate below
- false positives: 0 of 22 judged NO-GOs = 0% (ok 22 · fp 0)
- false negatives: 0 of 3 judged GO rows = 0%
- NO-GO causes: setup 4 · checks 2 · domain 16 · other 0 (of 22 NO-GO rows)
- calibration: 0 of 22 correct NO-GOs were marked 'better GO' by the human (0 of 22 judged = 0%)
- top exclusion reasons: EX-GUARD 14 · EX-CONTRACT 9 · EX-PAY 7 · EX-GDPR 6 · EX-SEC 4
```

```
retro-compliance: .
  period: all recorded rows (no report marker, no --since)
  run-log rows: 7 (docs/architecture/run-log.md)
    per skill: wai-pr-review 7
  gate-ledger verdicts: 26 — GO 3 · NO-GO 22 · UNKNOWN 1 · MOOT 0 (docs/architecture/gate-ledger.md)
  merged PRs: 5 merge commit(s), 5 with a readable PR number (git log --merges)
  traced share: 5 of 5 merged PRs carry a gate verdict in the period = 100% (raw: merge commits 5 · ledger rows 26 · run-log rows 7)
```

### What held

- **The gate was never wrong, over a fully judged record**: false positives 0 of 22 judged
  NO-GOs, false negatives 0 of 3 judged GO rows — and unusually, the denominator is complete:
  outcome coverage is 26 of 26 rows (100%, human-tagged). Small GO denominator (3), stated as
  such (artifact: the gate report above; the tags are the human's, set 2026-08-14).
- **Absence of failure, with its denominator**: since v0.1.0 (2026-08-10) every merged PR —
  13 of them, #15 through #26 — landed with CI green on its merge commit, and `main` stayed
  continuously releasable; two releases (v0.1.0, v0.2.0) were cut from it (artifacts: CI runs on
  the merge commits; the two tags).
- **The loop closed on the suite's own tooling twice in the period**: the run log's first row was
  written by the gate run of the PR that introduced the run log (#22), and this report's cadence
  trigger fired from the marker mechanism introduced in #17 (artifacts: run-log.md row 1; doctor
  advisory above).

### What broke

- **The run log's model-written tier is empty in the suite's own repo**: all 7 rows are
  `wai-pr-review` — written by `merge-gate.sh`, a script. Zero rows from the prose-contract tier
  (implementation, testing, audits, retro until this row), while the work of those skills
  demonstrably happened (13 merged PRs). This reproduces the field finding of issue #29
  ("self-logging that depends on the model is not a measurement") in the suite's home repo —
  raw: 7 of 7 rows script-written, 0 model-written.
- **The traced share's population is structurally wrong for this repo**: 5 of 5 = 100% covers
  only the merge-commit era (PRs #1–#6); the 10 squash-merged PRs since (#15–#26) are invisible
  to `git log --merges`, and among them are the two PRs known to have merged without a gate row
  (#15, #16 — session-limit loss, permanent). The honest reading: the one population where the
  metric can see is clean; the population containing the known gap is the one it cannot see.
  That is issue #24, now with field confirmation — a rate over the wrong set, not an imprecise
  rate.
- **One row stays outside every ratio**: PR #19's UNKNOWN verdict, tagged `need inspection` —
  not in the tag vocabulary, so gate-stats correctly drops it from all rates and says so. It
  awaits the human's resolution (raw: 1 of 26 rows unmatched).

### Calibration

- 0 of 22 correct NO-GOs were marked "better GO" — the human wanted every hold, including the
  three EX-PAY verdicts on #26 whose trigger was the classifier's `*token*`/`*ledger*` lexical
  match on release notes (tagged `ok`, not `fp`). The gate is currently not just correct but
  wanted. Counter-reading, from the field in the same week: a foreign repo measured the citation
  channel of the same classifier producing standalone false alarms (issue #30) — this repo's 0%
  does not generalize; it measures this repo's tolerance, and the domain cause dominates
  (16 of 22 NO-GO rows).

### Compliance — did the work leave a trace

- Per-skill run counts: `wai-pr-review` 7 — and nothing else (run-log rows: 7). The gate's own
  attendance is complete because a script writes it; every other skill's attendance is missing
  because a prompt asks for it. A low traced share is a finding about the suite's prompt
  contracts, not about the people — and here it is 0 for the model tier, the strongest possible
  version of that finding. Issue #29 carries the fix (mechanical invocation denominator).

### Collaboration

not derived — no artifact exists (a question trace is the prerequisite; nothing here is recalled)

### Outcomes (the human's column)

| finding | outcome |
|---|---|
| Model-written run-log tier empty (0 of 7 rows) — fix proposed in #29 | |
| Traced share blind to the squash population incl. the known #15/#16 gap — fix proposed in #24 | |
| PR #19 UNKNOWN row tagged `need inspection`, outside all ratios | |
| Classifier citation channel: 0 fp here, standalone false alarms in the field — dial proposed in #30 | |

### ▶ Recommended next

1. **#29** (`wai` / `wai-implementation`) — the invocation denominator: the one change that makes
   the next retro's compliance section a measurement instead of a zero.
2. **#24** (`wai-retro` / `wai-implementation`) — subject-parsing or gh-fallback for merged-PR
   derivation, so the traced share sees the population that contains the known gaps; acceptance
   sharpened: `open-items.sh` and `retro-compliance.sh` must agree on "merged".
3. **#30** (`wai` / the human) — the citation-widening dial: a guardrail change, so the decision
   is the human's; the field numbers are in the issue.
4. Resolve the `need inspection` row (#19, UNKNOWN) — tag it `ok`/`fp`/`nil` or leave it named;
   it is the last row outside the ratios.
